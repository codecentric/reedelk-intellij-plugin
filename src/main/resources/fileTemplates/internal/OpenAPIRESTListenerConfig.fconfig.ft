{
  "id": "${id}",
  "title": "${title}",
  "implementor": "com.reedelk.rest.component.RESTListenerConfiguration",
  "host": "${host}",
  "port": "${port}",
  "protocol": "HTTP",
  "basePath": "${basePath}",
  "keepAlive": true,
  "openApi": ${openApiObject}
}
