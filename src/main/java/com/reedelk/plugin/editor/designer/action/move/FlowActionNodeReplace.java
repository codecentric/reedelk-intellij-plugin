package com.reedelk.plugin.editor.designer.action.move;

import com.reedelk.plugin.graph.node.GraphNode;

public class FlowActionNodeReplace extends BaseActionNodeReplace {
    public FlowActionNodeReplace(GraphNode from, GraphNode to) {
        super(from, to);
    }
}
