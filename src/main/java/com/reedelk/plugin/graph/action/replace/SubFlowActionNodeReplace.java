package com.reedelk.plugin.graph.action.replace;

import com.reedelk.plugin.graph.node.GraphNode;

public class SubFlowActionNodeReplace extends FlowActionNodeReplace {
    public SubFlowActionNodeReplace(GraphNode from, GraphNode to) {
        super(from, to);
    }
}
